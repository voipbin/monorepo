apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bin-manager
commonLabels:
  app: bin-manager

resources:
  - namespace.yml
  - deployment.yml
  - service.yml

images:
- name: talk-manager-image
  newName: talk-manager
  newTag: latest
