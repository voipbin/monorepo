# Sphinx documentation build dependencies for bin-api-manager/docsdev.
# Install with: pip install -r requirements.txt
#
# The built HTML in docsdev/build/ is committed to the repo and served from
# docs.voipbin.net, so these dependencies are only needed when regenerating
# the HTML locally.

# The committed HTML under docsdev/build/ is generated with Sphinx 9.1.x,
# which requires Python 3.12 or newer. Build the docs in a Python >=3.12
# environment so the regenerated HTML matches what is committed.
Sphinx>=9.1,<10
furo>=2025.12.19
sphinxcontrib-youtube>=1.4.1
