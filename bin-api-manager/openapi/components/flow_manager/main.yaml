components:
  schemas:
    ActionType:
      $ref: 'action_type.yaml'
    Action:
      $ref: 'action.yaml'

    ActiveflowReferenceType:
      $ref: 'activeflow_reference_type.yaml'
    ActiveflowStatus:
      $ref: 'activeflow_status.yaml'
    Activeflow:
      $ref: 'activeflow.yaml'

    FlowType:
      $ref: 'flow_type.yaml'
    Flow:
      $ref: 'flow.yaml'