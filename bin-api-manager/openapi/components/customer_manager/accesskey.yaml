type: object
properties:
  id:
    type: string
  customer_id:
    type: string
  name:
    type: string
  detail:
    type: string
  token:
    type: string
  tm_expire:
    type: string
    description: Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
  tm_create:
    type: string
    description: Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
  tm_update:
    type: string
    description: Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
  tm_delete:
    type: string
    description: Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
