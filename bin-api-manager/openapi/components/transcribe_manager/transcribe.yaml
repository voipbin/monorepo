type: object
properties:
  id:
    type: string
    description: Transcribe id
  customer_id:
    type: string
    description: Customer ID
  reference_type:
    $ref: '#/components/schemas/TranscribeReferenceType'
  reference_id:
    type: string
    description: Call/Conference/Recording's ID
  status:
    $ref: '#/components/schemas/TranscribeStatus'
  language:
    type: string
    description: BCP47 type's language code.
  direction:
    $ref: '#/components/schemas/TranscribeDirection'
  tm_create:
    type: string
    description: Timestamp when created. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
=  tm_update:
    type: string
    description: Timestamp when updated. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
=  tm_delete:
    type: string
    description: Timestamp when deleted. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
