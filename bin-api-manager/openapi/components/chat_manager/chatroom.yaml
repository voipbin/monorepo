type: object
properties:
  id:
    type: string
    description: Resource identifier.
  customer_id:
    type: string
    description: Resource's customer ID.
  owner_type:
    type: string
  owner_id:
    type: string
    description: Resource's owner ID.
  type:
    $ref: '#/components/schemas/ChatroomType'
  chat_id:
    type: string
    description: Associated chat ID.
  room_owner_id:
    type: string
    description: Chat room's owner agent ID.
  participant_ids:
    type: array
    items:
      type: string
    description: List of participant agent IDs.
  name:
    type: string
    description: Name of the chat room.
  detail:
    type: string
    description: Detailed information about the chat room.
  tm_create:
    type: string
    description: Timestamp when the chat room was created. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
  tm_update:
    type: string
    description: Timestamp when the chat room was last updated. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
  tm_delete:
    type: string
    description: Timestamp when the chat room was deleted. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
