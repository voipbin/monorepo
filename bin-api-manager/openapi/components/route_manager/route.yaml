type: object
properties:
  id:
    type: string
    description: "The unique identifier for the route."
  customer_id:
    type: string
    description: "The customer ID associated with the route."
  name:
    type: string
    description: "The name of the route."
  detail:
    type: string
    description: "The details about the route."
  provider_id:
    type: string
    description: "The unique identifier of the provider associated with this route."
  priority:
    type: integer
    description: "The priority of the route, used for routing decisions."
  target:
    type: string
    description: "The target destination for the route (e.g., country code or 'all')."
  tm_create:
    type: string
    description: Timestamp when the route was created. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
  tm_update:
    type: string
    description: Timestamp when the route was last updated. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
  tm_delete:
    type: string
    description: Timestamp when the route was deleted. Custom date-time format(YYYY-MM-DD HH:mm:ss.ssssss)
