post:
  summary: Create a new activeflow
  description: Endpoint to create a new activeflow.
  tags:
    - Activeflow
  requestBody:
    description: Request body to create a new activeflow.
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            id:
              type: string
              description: >
                Optional client-supplied UUID for the new activeflow. If omitted, the server generates one.
            flow_id:
              type: string
              description: >
                UUID of an existing flow to run. Obtain it from the id field of a GET /flows or POST /flows response.
                Provide either flow_id or an inline actions array.
            actions:
              type: array
              description: >
                Inline action list to run when no flow_id is provided. Provide either flow_id or actions.
              items:
                $ref: '#/components/schemas/FlowManagerAction'
            variables:
              type: object
              additionalProperties:
                type: string
              description: >
                Optional flat key-value context seeded into the created activeflow as runtime variables,
                readable in the flow via ${key}. String values only. Reserved keys starting with
                'voipbin.' are ignored. Max 100 keys, 64KB total, 32KB per value.
              example:
                campaign_id: "summer-2026"
                customer_name: "Jane Doe"
  responses:
    '200':
      description: activeflow created successfully.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/FlowManagerActiveflow'
    '400':
      $ref: '#/components/responses/BadRequest'
    '401':
      $ref: '#/components/responses/Unauthenticated'
    '500':
      $ref: '#/components/responses/InternalError'

get:
  summary: List all activeflows
  description: Retrieve a list of all activeflows.
  tags:
    - Activeflow
  parameters:
    - $ref: '#/components/parameters/PageSize'
    - $ref: '#/components/parameters/PageToken'
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/CommonPagination'
              - type: object
                properties:
                  result:
                    type: array
                    items:
                      $ref: '#/components/schemas/FlowManagerActiveflow'
    '401':
      $ref: '#/components/responses/Unauthenticated'
    '500':
      $ref: '#/components/responses/InternalError'
