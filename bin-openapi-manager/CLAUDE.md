# bin-openapi-manager

Centralized OpenAPI specification repository and Go type code-generation tool. **No runtime service** — no binary, no RabbitMQ queue, no deployment. Consumed at compile time by all other `bin-*-manager` services.

> Cross-cutting rules (verification workflow, branch/commit format, worktrees, Alembic, RST sync) live in the root [CLAUDE.md](../CLAUDE.md). This file covers only what is specific to `bin-openapi-manager`.

## Key facts

- **No runtime.** No `cmd/` entrypoint, no listen queue, no subscriptions.
- **Single output artifact**: `gens/models/gen.go` — generated by `oapi-codegen` from `openapi/openapi.yaml`.
- **All other services import** `monorepo/bin-openapi-manager/gens/models`.
- Changing a schema here can break every consumer — always verify with `go build ./...` in `bin-api-manager`.

## Codegen pipeline

```
openapi/openapi.yaml  +  openapi/paths/**/*.yaml
        |
        v  go generate ./...
gens/models/gen.go   (DO NOT EDIT manually)
```

Generator: `oapi-codegen` v2, config at `configs/config_model/config.generate.yaml` (generates `models` package, types only).

## Common commands

```bash
# Validate spec
oapi-codegen -config configs/config_model/config.generate.yaml openapi/openapi.yaml > /dev/null

# Regenerate types
go generate ./...

# Check consumer build
cd ../bin-api-manager && go build ./...
```

## OpenAPI spec rules (mandatory for all new/modified fields)

1. **`oneOf` for polymorphism** — no `additionalProperties: true` on type-discriminated objects.
2. **Format structured strings** — UUIDs: `format: uuid`; timestamps: `format: date-time`; phone numbers: `pattern`; enums: `enum` keyword.
3. **Provenance in ID descriptions** — state the source endpoint: `"The X returned from POST /y response."`
4. **Realistic examples on every leaf** — never use `"string"`, `null`, or `"user_id"`.
5. **`minItems: 1` on required arrays.**

## Schema validation rule

OpenAPI schemas must match the `WebhookMessage` struct in each service (`models/<resource>/webhook.go`), not the internal model struct. Only externally exposed fields belong in the spec.

## Adding a new resource

1. Create `openapi/paths/<resource>/main.yaml` and `id.yaml`.
2. Add `$ref` entries in `openapi/openapi.yaml` under `paths:`.
3. Add schemas under `components/schemas:` prefixed with the service name (e.g., `MyManagerMyResource`).
4. Run `go generate ./...`.
5. Commit `gens/models/gen.go` with spec changes.

## Further reading

- [docs/architecture.md](docs/architecture.md)
- [docs/usage.md](docs/usage.md)
