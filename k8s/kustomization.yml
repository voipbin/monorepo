apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bin-manager

resources:
  - namespace.yml
  - secret.yml
  - deployment.yml
  - service.yml

images:
- name: transcribe-manager-image
  newName: transcribe-manager
  newTag: latest

