apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bin-manager
commonLabels:
  app: bin-manager

resources:
  - namespace.yml
  - secret.yml
  - deployment.yml
  - service.yml

images:
- name: route-manager-image
  newName: route-manager
  newTag: latest
