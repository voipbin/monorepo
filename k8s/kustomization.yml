apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: bin-manager
commonLabels:
  app: api-manager

resources:
  - namespace.yml
  - deployment.yml
  - service.yml

images:
- name: api-manager-image
  newName: api-manager
  newTag: latest
