#!/usr/bin/env bash
# pre-commit hook placeholder
#
# Checks previously here have been moved to Claude Code PostToolUse hooks
# in .claude/settings.json for earlier feedback during the work session.
#
# Install: git config core.hooksPath .githooks

exit 0
