apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bin-manager

resources:
  - namespace.yml
  - deployment.yml

images:
  - name: rag-manager-image
    newName: rag-manager
    newTag: latest
