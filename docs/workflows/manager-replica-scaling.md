# Scaling a bin-*-manager to 2 Replicas

> **Quick Reference:** This is the durable, service-agnostic recipe.
> Rollout state (gating conditions, per-service tracker) lives in
> [docs/plans/2026-08-28-bin-manager-two-replica-rollout-design.md](../plans/2026-08-28-bin-manager-two-replica-rollout-design.md)
> — **read its gating conditions before scaling anything.**
> Reference implementation:
> [bin-schedule-manager/komodo/docker-compose.yml](../../bin-schedule-manager/komodo/docker-compose.yml)
> ([PR #1212](https://github.com/voipbin/monorepo/pull/1212), live-verified 2026-08-27).
> Service-specific notes (e.g. schedule-manager's backup-path safety)
> stay in each service's own `docs/operations.md` — this document only
> carries what applies to every service.

## 1. Eligibility checklist — run per service, before touching its compose

A service may only be scaled when every item passes. The current
per-service answers are tracked in the rollout design doc; re-verify
against the code, not the tracker, if any time has passed.

1. **No per-pod queue.** A volatile per-pod RPC queue
   (`bin-manager.<svc>-<host_id>.request`) means later RPCs must reach
   the *owning* replica — competing consumers on a shared queue break
   that. See [docs/patterns/per-pod-queues.md](../patterns/per-pod-queues.md).
   (Blocked today: pipecat, transcribe.)
2. **No container-name-dependent env/DNS.** Anything that resolves the
   service's own (or a sidecar's) fixed `container_name` via Docker DNS
   breaks when the name is removed. (Blocked today: tts —
   `POD_IP=voipbin-tts-manager-http` points at its http sidecar's
   container name; pipecat — `POD_IP=pipecat-manager`. transcribe's
   `${POD_IP:-127.0.0.1}` fallback looks similar but its real HostID is
   a random UUID — a different problem; see that service's docs.)
3. **No external naming binding.** monorepo-etc's
   `infra-caddy/config/Caddyfile` targets `voipbin-api-manager`
   (`:17-22`, the current name since VOIP-1362 retargeted it from the
   install/-era `voipbin-api-mgr`) and `voipbin-hook-manager`
   (`:77-82`) literally as reverse_proxy upstreams. Scaling either
   service requires a Caddy-side companion change first. (api
   additionally needs a per-pod `streamData` state redesign — the Caddy
   fix alone is not sufficient for it.)
4. **Per-container mutable state is concurrency-safe.** If the service
   writes to a bind mount or other shared location, confirm two
   replicas cannot corrupt it (e.g. schedule-manager's backup dir is
   safe because the backup job is claimed via a shared
   competing-consumer queue + Redis lock + DB CAS — reasoning in
   [its operations.md](../../bin-schedule-manager/docs/operations.md)).
5. **Advertised addresses resolve per container.** If the service hands
   out its own address (media sockets etc.), it must derive it per
   container — use the `AdvertiseIP()` pattern
   (`bin-api-manager/internal/nethandler`, POD_IP override →
   auto-detect → fail fast). Its package doc records the promotion
   rule: if 3+ services end up needing it, promote it to
   bin-common-handler.
6. **Work is claimed, not assumed.** Background loops must coordinate
   via a shared competing-consumer queue, or Redis lock + DB CAS, or an
   equivalent — never "I am the only instance". (Blocked today: route's
   provider healthcheck loop, contact's in-process lock; timeline needs
   drop metrics first.)

Not a concern: **host-port collisions** — no `bin-*-manager` compose
publishes host ports, so replicas cannot conflict there.

## 2. The compose change

Two edits to `<service>/komodo/docker-compose.yml`:

```yaml
services:
  <service>:
    # container_name intentionally removed - Docker Compose rejects a
    # fixed container_name together with deploy.replicas > 1
    # ("container name must be unique").
    deploy:
      replicas: 2
```

- Delete the `container_name: voipbin-<service>` line (keep a short
  comment saying why, as above — the reference implementation does).
- Add the `deploy.replicas` block.

Resulting container names are generated by Compose:
`bin-<project>-<service>-1` / `-2` (e.g.
`bin-schedule-manager-schedule-manager-1`) — confirmed against the live
bm-nyc-01 deploy on 2026-08-27. Prometheus needs no scrape-config
change: the `voipbin-managers` job uses DNS SD on the compose service
name, and Docker's embedded DNS returns every replica's IP.

## 3. Deploy and downtime

Two distinct kinds of downtime — do not conflate them:

- **Redeploy blip (applies fleet-wide).** Non-Swarm Compose does not do
  rolling updates, so a redeploy is expected to recreate both replicas
  together. This was accepted deliberately (CEO/CTO decision, see the
  rollout design doc) in place of a zero-downtime A/B scheme. Schedule
  deploys for low-traffic windows and announce them. Note the exact
  blip pattern has not been measured yet — what the pilot verified is
  the scale-up mechanism itself (two containers start, naming, healthy),
  not the behavior of a subsequent redeploy.
- **Naming-cutover downtime (service-specific).** Only services with an
  external naming binding (checklist item 3: api, hook) additionally
  pay a cutover window when the name their upstream targets changes.

## 4. Verifying a deploy — the CI gate does NOT prove 2-of-2

`check_stack_running` in `.circleci/scripts/komodo-api-deploy.sh`
(`:367-373`) reads `.container.state` **per compose service, not per
container** — with replicas it can report success while only one of two
replicas is running. A green deploy therefore proves at most 1-of-2.

After every deploy of a scaled service, verify manually:

```bash
# On bm-nyc-01: expect exactly 2 running containers
docker ps --filter name=<service> --format '{{.Names}}\t{{.Status}}'
```

```promql
# In Prometheus: expect 2
count(up{job="voipbin-managers", service="<service>"})
```

Also give the service's Grafana dashboard a quick sanity pass:
per-instance panels now show two series per service (harmless — proven
by the schedule-manager pilot — but confirm nothing renders wrong).

## 5. Known fleet-wide gaps (tracked in the rollout design doc — do not re-derive per service)

- **1-of-2 degradation alert: `ReplicaDegraded`** (warning, `for: 10m`,
  monorepo-etc alert-rules.yml) covers the replica-scaled services —
  its regex enumerates them, so **scaling a service up or down requires
  updating that regex and its tests**. Section 4's manual check remains
  the verification at deploy time (the alert's 10m window deliberately
  ignores deploy churn).
- **Runbook naming.** monorepo-etc's alert-rules.yml runbooks describe
  both container-name regimes (replica-scaled
  `bin-<svc>-manager-<svc>-manager-N`, single-replica
  `voipbin-<svc>-manager`); the `docker ps --filter name=` substring
  match works for both.
- **Redeploy blip pattern unmeasured** (see section 3).
- **No resource limits, deliberately** (2026-08-28). cAdvisor on
  bm-nyc-01 exposes only system cgroup slices — no per-container series
  exist to size limits from — and one-shot `docker stats` shows
  16-69MiB per manager against ~240GB free host memory. Inventing
  limits without data risks OOMKills for no benefit; revisit only after
  cAdvisor per-container metrics are fixed (tracked separately).

## 6. Rollback

Revert only the affected service's compose file. Each service is an
independent Komodo Stack with an independent CircleCI path-filtered
pipeline — one service's failure neither blocks nor rolls back any
other, even when many were changed in one commit.

## References

- [bin-schedule-manager/komodo/docker-compose.yml](../../bin-schedule-manager/komodo/docker-compose.yml) — reference implementation
- [bin-schedule-manager/docs/operations.md](../../bin-schedule-manager/docs/operations.md) — pilot's service-specific notes
- [docs/plans/2026-08-28-bin-manager-two-replica-rollout-design.md](../plans/2026-08-28-bin-manager-two-replica-rollout-design.md) — gating + per-service tracker
- [docs/patterns/per-pod-queues.md](../patterns/per-pod-queues.md), [docs/patterns/per-pod-liveness-preflight.md](../patterns/per-pod-liveness-preflight.md)
